---
"@cerberauth/tokens": minor
"@cerberauth/ui": minor
---

Add `warning` semantic color token (`--token-warning`, `-fg`, `-fixed`, `-fixed-fg`, light + dark) alongside the existing primary/secondary/destructive/success/accent tokens, plus a matching `warning` variant on `Alert` and `Badge`.
